apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: plaid
    plaid: superset
    release: {{ .Release.Name }}
spec:
  ports:
  - port: {{ .Values.superset.port }}  
    targetPort: 9000 #{{ .Values.superset.targetPort }}  
    protocol: TCP
    name: http
  selector:
    plaid: superset
    release: {{ .Release.Name }}
